name: bocal-smtpd

services:
  cert-fetcher:
    image: bitwarden/bws:latest
    environment:
      - BWS_ACCESS_TOKEN
      - BWS_PROJECT_ID
      - BWS_PRIVATEKEY_ID
      - BWS_FULLCHAIN_ID
      - TLS_CERT_PATH=/app/certs/fullchain.pem
      - TLS_KEY_PATH=/app/certs/privatekey.pem
    volumes:
      - tls_certs_volume:/app/certs
      - ./fetch-certs.sh:/usr/local/bin/fetch-certs.sh:ro
    command: ["/usr/local/bin/fetch-certs.sh"]

  bocal-smtpd:
    image: ghcr.io/mtlaso/bocal-smtpd:latest
    restart: unless-stopped
    volumes:
      - tls_certs_volume:/app/certs:ro
    environment:
      - TLS_CERT_PATH=/app/certs/fullchain.pem
      - TLS_KEY_PATH=/app/certs/privkey.pem
      - DATABASE_URL=${DATABASE_URL}
    depends_on:
      cert-fetcher:
        condition: service_completed_successfully
    networks:
      bocal_net:
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 465 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    scale: 3

  caddy:
    image: ghcr.io/mtlaso/bocal-caddy_l4:latest
    ports:
      - "587:587"
      - "465:465"
    volumes:
      - caddy_data:/data
      - ./Caddyfile.prod:/etc/caddy/Caddyfile
    networks:
      bocal_net:
    restart: unless-stopped

networks:
  bocal_net:
    ipam:
      driver: default
      config:
        - subnet: 172.28.0.0/16

volumes:
  tls_certs_volume:
  caddy_data:
